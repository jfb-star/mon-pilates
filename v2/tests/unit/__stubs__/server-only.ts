// Empty stub — Next.js's `server-only` marker has no runtime,
// it exists purely to make bundlers refuse to include server files
// in client bundles. In vitest there is no bundler split, so we
// alias this package to an empty module.
export {}
