// Twitter image reuses the root Open Graph image design.
// Re-exporting the OG default keeps both cards visually identical without
// duplicating the JSX.
export { default, alt, size, contentType } from "./opengraph-image"
